# Funcionalidades
* logs de commits
* interface para logs de commits
* interface push
* interface add
* interface commit

# Teste
* testar init (regex)
* testar clone (script)
* testar add (regex)
* testar commit (regex)
